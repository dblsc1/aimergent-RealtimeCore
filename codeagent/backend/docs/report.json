{
  "schema_version": 2,
  "module": "realtime_core",
  "role": "backend",
  "task": "realtime_core P3a：会话内核（上）——事实日志 + 游标投递（事件信封携 v / 存储端口 + 内存实现 + CAS / delivery publish-pull-ack-subscribe 复用 P2 longPoll / 四不变量 property 测 / 纯度门扩展 session scope）",
  "tier": "normal",
  "status": "approved",
  "summary": "[backend 自检通过的 candidate，待 reviewagent 正式审核；分支未合 main] 新增 src/session/ 四文件：信封 {streamId,seq,id,type,v,at,payload}（seq/at 日志层分配、v 版本字段即刻承载、payload 黑盒、信封冻结）；存储端口 + createMemoryLogStore（CAS append 整批原子 / read / getCursor / advanceCursor 只前进）；createDelivery（publish=append+wakeup.emit('appended')，pull/ack 分离 at-least-once，同 group 共享游标，subscribe 把注入的 P2 longPoll 组装成有事件即唤醒——零 transport import、零自制轮询）。四条不变量各有固定种子 property 测（含随机崩溃重建），worklog 附映射表。既有 72 测试零修改全绿，新增 38 全绿（110/110 串行），纯度门扩展 session scope 后 36/36 PASS。分支 feat/p3a-log-cursors 不合 main。",
  "git": {
    "protocol": "embedded-self-v2",
    "branch": "feat/p3a-log-cursors",
    "base": "c634568359fb28b73284dc7fe01acc1e2e49625e",
    "head": "SELF",
    "diff_mode": "contains",
    "changed_files": [
      "code/backend/src/session/errors.js",
      "code/backend/src/session/envelope.js",
      "code/backend/src/session/memory-log-store.js",
      "code/backend/src/session/delivery.js",
      "code/backend/src/session/memory-log-store.test.mjs",
      "code/backend/src/session/delivery.test.mjs",
      "code/backend/src/session/delivery.subscribe.test.mjs",
      "code/backend/src/session/log-cursors.property.test.mjs",
      "code/backend/reference/classroom-feed.ref.mjs",
      "code/backend/reference/classroom-feed.ref.test.mjs",
      "review/reviewcode/check-kernel-purity.mjs",
      "module_docs/contract.md",
      "module_docs/rules.md",
      "module_docs/handoff.md",
      "codeagent/backend/docs/worklog/2026-07-19-backend-p3a-log-cursors.md",
      "codeagent/backend/docs/report.json"
    ]
  },
  "contract": { "touched": false, "which": [], "consumes": [] },
  "cross_module_impact": [],
  "escalation": null,
  "tests": {
    "unit": { "passed": 110, "failed": 0, "cmd": "cd code/backend && node --test --test-concurrency=1 (串行)" },
    "e2e": { "passed": 0, "failed": 0, "flow": "无 e2e：库模块，无服务进程/对外端点" }
  },
  "compat_gate": {
    "existing_tests_unmodified": true,
    "existing_pass": 72,
    "new_pass": 38,
    "evidence": "git diff c634568..HEAD 对全部既有测试文件（transport/concurrency/queue/reference 的 P1+P2 用例）为空；72 既有零修改 + 38 新增 = 110 全绿"
  },
  "self_check": {
    "reviewcode_passed": true,
    "attempts": 1,
    "failed_gates": [],
    "passed": true,
    "detail": "check-kernel-purity.mjs 36 PASS / 0 FAIL（transport 16 项逐项与 P2 一致 + 新 session scope 20 项：import 不出目录零 transport / 零扩展领域词 scenario·question·skill·scene·round / 零 Date.now·Math.random / 零 db.transaction / ≤500 行；最大文件 delivery.js 127 行）；门内正则经正反样本抽查无误伤"
  },
  "invariants_mapping": [
    "不变量1（ack 序列=日志连续前缀）→ property①（300 种子×30 步影子模型 harness，每步 confirmed==shadowIds.slice(0,cursor)）+ property⑥ 加压",
    "不变量2（seq 连续无空洞 + CAS 恰好一个胜者）→ property②（150 种子双流连续性）+ property③（200 种子同快照 K 路，winners==1 且日志恰好多一批）+ property① 内嵌 casClash 步",
    "不变量3（游标只前进 + ack 不越已 pull 高水位）→ property④（200 种子端口层随机攻击）+ property⑤（200 种子越位 ack 恒 RangeError 且游标不动）+ property① 每步单调核对",
    "不变量4（随机崩溃仅凭 logStore 重建后不变量1 仍成立）→ property①（crash p=0.15）+ property⑥（crash p=0.5，100 种子×40 步）+ delivery.test 崩溃重建确定性用例"
  ],
  "deviations": [
    "longPoll 以能力注入（createDelivery({logStore, wakeup, longPoll})）而非 import——任务单'必须复用 P2 longPoll'与'session/ 零 transport import'两约束的交集解；subscribe 未注入即 TypeError",
    "游标违规（回退/越日志末尾/越高水位）用内建 RangeError，仅 CAS 冲突设专类 ConflictError（编程错误 vs 可重试并发事实，语义分开）；识别用 err.name 不强依赖 instanceof",
    "ack 同 seq 幂等 no-op（at-least-once 重投的自然结果）；回退与越位 throw；严格版可向后兼容放宽，反向不行",
    "publish 缺省'追加到尾'（尾指针缓存 + ConflictError 兜底重读重试一次），显式 expectedLastSeq 走严格 CAS 原样上抛",
    "信封 id 生成本地实现 evt-<clock>-<rand36>（与 queue/ids.js 同格式）而非跨目录 import——session/ 纯度门禁出目录，格式重复登记 P5 技术债",
    "调用方事件携带框架分配字段（seq/at/streamId）或未知键 = TypeError 严格拒绝；id 允许自带（跨系统去重），缺省生成",
    "subscribe 结算走 P1 原生 respond.settled(batch)，不新造 outcome 词汇（最小扩展面）",
    "lightweight 模式下单 agent 按任务单显式授权一并更新 module_docs（contract/rules/handoff）与 review/ 纯度门脚本；下游仍有独立 reviewagent 审 candidate",
    "本仓无 git remote（dev 孵化局部仓，与 P1/P2 同状态）——candidate 停在本地 commit，无 push 环节"
  ],
  "tech_debt": [
    "sealEnvelopes 的 id 生成与 queue/ids.js 格式重复实现（纯度门禁跨目录 import 所致，P5 统一）",
    "符号命名带领域味（sessionLockKey/orderedSessionEvents/genEventId）P5 中性化候选（破 API）——沿用 P1 债",
    "ordering.js 无专属测试——沿用 P1 债",
    "locks.js 形参 skillId 领域词——沿用 P1 债"
  ]
}
