# realtime_core · 对外接口契约

> 本文件是 realtime_core 对外行为的**唯一事实**。完整治理模式下，改本文件必须先提 CR 给项目 arbiter，批准后先改这里、再改代码。轻量模式尚未发布契约时可直接编辑；首次发布后的破坏性变更必须先评估消费方并留变更记录。禁止悄悄删除既有承诺。

> 轻量且尚无外部消费方时可暂保留本模板；首次开放外部调用前，必须删掉所有占位文字并填成真实契约。

## 契约索引声明（provides / consumes）

```yaml
provides:
  - id: （对外能力的稳定 ID）
    summary: （一句话说明提供什么）
consumes:
  - id: （平台契约 ID；无依赖则写 []）
    contract: （例如 {{FRAMEWORK_ROOT}}/modules/shared_service/module_docs/contract.md）
    purpose: （用途）
```

## 对外 API

| 方法 | 路径 | 入参 | 出参 | 备注 |
|---|---|---|---|---|
| （迁移时填写） | | | | |

## 入口与路由

- nginx 公开前缀：`/realtime_core/`（创建后确认或更正）
- 内部服务名 / 端口：（填写）

## 依赖的外部契约

| 依赖 | 契约位置 | 用途 |
|---|---|---|
| （只保留实际依赖的平台契约） | | |

## 数据与存储

（本模块拥有哪些数据、如何通过 env 指定 Git 外的 data root、备份与清理策略。新增数据文件必须声明属主/备份/清理三件事。）

## 配置与密钥

> 同步维护根目录 `.env.staging.example`；只允许变量名和无敏占位值进 Git。

| 环境变量 | 必填 | 用途 | 安全约束 |
|---|---|---|---|
| （创建后填写） | | | 仅写占位说明，不写真值 |

## 变更记录

| 日期 | CR | 变更 |
|---|---|---|
