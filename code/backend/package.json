{
  "name": "@aimergent/realtime-core",
  "version": "1.0.0",
  "private": true,
  "description": "Domain-agnostic realtime/state-machine kernel: long-poll lifecycle reducer, command dispatch, channel broadcast, keyed locks, session kernel (event log + cursor delivery + decide/evolve aggregate + event versioning + crash replay), declarative state-machine tool. Zero runtime deps. Formal contract: module_docs/contract.md (v1.0.0).",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
