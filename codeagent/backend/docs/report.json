{
  "schema_version": 2,
  "module": "realtime_core",
  "role": "backend",
  "task": "realtime_core P4：defineMachine 声明式转移表工具（平表状态机 states/on/target/guard/final + 纯谓词守卫 + 定义期全面校验响亮 throw 带 id/位置 + transition/can/assertState/finalStates；不做层级/并行/actor/actions/延迟；纯度门加 machine scope；参考示例手写 phase if/else 改表驱动守卫等价证明）",
  "tier": "hard",
  "status": "approved",
  "summary": "[backend 自检通过的 candidate，待 reviewagent 正式审核；分支 feat/p4-define-machine 未合 main] 新增 src/machine/define-machine.js（270 行零 import）：defineMachine(spec)→Object.freeze 不可变纯机器，transition(非法响亮 throw IllegalTransitionError)/can(不抛，can===true⟺transition 成功)/states·finalStates(冻结枚举)/initial/assertState(裸串逃逸断言)。核心价值=定义期全面校验：19 类非法定义在 defineMachine() 调用时立即抛 MachineDefinitionError，错误信息带 machine id 与具体位置(如 states.asking.on.ANSWER.target)。词汇照抄 XState(states/on/target/guard/initial/final/guards)但只做平表；明确不做层级/并行/actor/entry-exit-actions/延迟转移/字符串 target 简写(YAGNI 记 rules.md)。guard 纯谓词(ctx,event)→boolean，抛异常=编程错误原样上抛不吞。JS 对象字面量静默折叠重复键→运行时无法检测重复键，改以未知键严格拒绝作响亮校验等价收益(拼错的 gaurd/onn 定义期照亮)。参考示例 reference/classroom-aggregate.ref.mjs 把手写 phase 守卫(state.phase==='closed'/ANSWERING_PHASES.has)改 CLASSROOM_MACHINE.can(phase,EVENT) 表驱动，machine 只守卫不产事件不折叠态(组合边界)，既有 4 参考测试零修改全绿=等价证明。既有 153 测试零修改全绿(git diff 仅 ref.mjs 被改无既有测试被动)，新增 34 全绿(machine 单测 32+property 2)，全量 187/187 串行。纯度门抽 checkStrictScope helper 加 machine scope，56→61 PASS/0 FAIL。分支不合 main。",
  "git": {
    "protocol": "embedded-self-v2",
    "branch": "feat/p4-define-machine",
    "base": "c6b1a97fc258ddf7f496e43ab8c5f0bec63a2b33",
    "head": "SELF",
    "diff_mode": "contains",
    "changed_files": [
      "code/backend/src/machine/define-machine.js",
      "code/backend/src/machine/define-machine.test.mjs",
      "code/backend/src/machine/define-machine.property.test.mjs",
      "code/backend/reference/classroom-aggregate.ref.mjs",
      "review/reviewcode/check-kernel-purity.mjs",
      "module_docs/contract.md",
      "module_docs/rules.md",
      "module_docs/handoff.md",
      "codeagent/backend/docs/worklog/2026-07-19-backend-p4-define-machine.md",
      "codeagent/backend/docs/report.json"
    ]
  },
  "contract": { "touched": false, "which": [], "consumes": [] },
  "cross_module_impact": [],
  "escalation": null,
  "tests": {
    "unit": { "passed": 187, "failed": 0, "cmd": "cd code/backend && node --test --test-concurrency=1 (串行)" },
    "e2e": { "passed": 0, "failed": 0, "flow": "无 e2e：库模块，无服务进程/对外端点。三层全链路自证由 reference/classroom-aggregate.ref.test.mjs 承担（P4 起其 decide 守卫改用表驱动 machine.can，行为不变）" }
  },
  "compat_gate": {
    "existing_tests_unmodified": true,
    "existing_pass": 153,
    "new_pass": 34,
    "evidence": "git diff --name-only --no-renames c6b1a97 -- code/ 仅 reference/classroom-aggregate.ref.mjs 一个文件（任务单 §3 显式授权改造，且其配套 4 个参考测试零修改全绿=等价证明）；无一既有测试文件被改。153 既有零修改 + 34 新增 = 187 全绿"
  },
  "self_check": {
    "reviewcode_passed": true,
    "attempts": 1,
    "failed_gates": [],
    "passed": true,
    "detail": "check-kernel-purity.mjs 61 PASS / 0 FAIL（transport 16 + session 40（8 文件×5）+ machine 5（P4 新增 1 文件×5，import 不出目录零 transport / 零扩展领域词 / 零 Date.now·Math.random / 零 db.transaction / ≤500 行；define-machine.js 270 行）。session/machine 共用 checkStrictScope helper（DRY 重构）；states/events 通用词天然领域无关）"
  },
  "definition_time_validation_coverage": [
    "spec 非对象 → MachineDefinitionError",
    "id 非非空字符串（缺/空/非串）→ throw",
    "spec 顶层未知键 → throw（含 id + spec + 键名）",
    "states 非对象/为空 → throw",
    "状态名为空字符串 → throw",
    "initial 非非空字符串 → throw",
    "initial 不在 states → throw（含 id + initial + 名字）",
    "状态定义非对象 → throw",
    "状态未知键（onn）→ throw（含 states.<s> + 键名）",
    "type 非 'final' → throw（含 states.<s>.type + 值）",
    "final 状态声明 on → throw（终态无出边，含 states.<s>.on）",
    "on 非对象 → throw",
    "转移定义非对象 → throw（含 states.<s>.on.<e>）",
    "转移未知键（拼错 gaurd）→ throw",
    "target 非非空字符串 → throw",
    "target 指向不存在的状态 → throw（含 id + ...target + 名字）",
    "guard 非字符串 → throw",
    "guard 引用未在 guards 表定义 → throw（含 id + ...guard + guard 名）",
    "guards 非对象 / guard 非函数 → throw"
  ],
  "invariants_mapping": [
    "不变量1（状态封闭性：任意事件序列下 transition 结果恒 ∈ states 全集，且 can=true⟺transition 成功）→ property①（200 种子×40 步，含机器不认识的 NOISE 噪声事件搅动 event-not-handled 路径，每步断言转移后状态 ∈ stateSet、changed 语义、can/transition 一致性）",
    "不变量2（终态吸收性：进入 final 后任何事件恒 throw 且 can=false，机器不可复活）→ property②（120 种子，随机走到终态或强制 CLOSE 保证覆盖，终态后逐事件断言 can=false 且 transition 必抛 IllegalTransitionError）"
  ],
  "reference_refactor_tradeoff": "取舍=改原文件而非新增变体：改原文件让'既有 4 参考测试零修改全绿'直接成为'表驱动与手写等价'的证明，最干净；新增变体要复制组装代码、留两套 decide 违 DRY。改造=3 处手写 phase 守卫（push-question 的 state.phase==='closed'、submit-answer 的 ANSWERING_PHASES.has、close 的 state.phase==='closed'，含 v2 override 的 submit-answer）全下沉到 CLASSROOM_MACHINE.can(phase,EVENT)，删除 ANSWERING_PHASES 常量。等价证明=转移表逐格复刻原逻辑（PUSH_QUESTION/CLOSE 存在于 idle/asking/awaiting 不在 closed→can=false→保留原 reject 码；SUBMIT_ANSWER 只在 asking/awaiting=复刻 ANSWERING_PHASES），classroom-aggregate.ref.test.mjs 一字未改 4 用例全绿。machine 只守卫（can）不产事件不折叠态——decide 仍产事件、evolve 仍推进 phase（组合边界）。",
  "deviations": [
    "重复键无法运行时检测：JS 对象字面量静默折叠重复键（{a:1,a:2}→{a:2}），运行时拿不到重复信息；保守替代=对状态定义(只许 on/type)/转移定义(只许 target/guard)/spec 顶层做未知键严格拒绝，取响亮校验的等价收益，已在 contract/rules/worklog 记录",
    "guard 签名 (ctx, event) 照抄 XState 顺序，但 event 传入事件名字符串（与 transition(state,event) 一致，本工具平表事件即名字），非 XState 事件对象——已写入 contract guard 契约",
    "guard 抛异常不吞：guard 应纯谓词，抛异常=编程错误，transition 与 can 均原样上抛（不 try/catch 吞成 false），测试钉死",
    "字符串 target 简写不做（XState 支持 on:{E:'target'}）：YAGNI，只支持 {target,guard?} 对象形式，非对象转移定义响亮 throw，记 rules.md",
    "assertState 复用 IllegalTransitionError(reason='unknown-state') 而非新设类：语义都是状态不合法，少一个导出面",
    "纯度门重构：session/ 的 5 项严格检查抽成 checkStrictScope(dir,label,banner) helper，session/ 与 machine/ 共用（DRY）；行为对 session/ 逐字不变（40 项照旧 PASS）",
    "lightweight 单 agent 按任务单显式授权一并更新 module_docs（contract/rules/handoff）+ 纯度门脚本；下游仍有独立 reviewagent 审 candidate",
    "本仓 dev/ 孵化局部仓无 git remote——candidate 停在本地 commit 无 push 环节（与 P1/P2/P3a/P3b 同状态）"
  ],
  "tech_debt": [
    "defineMachine YAGNI 项（层级/并行状态、entry/exit actions、invoke/actor、延迟转移、字符串 target 简写）P5 按真实需求评估是否纳入 v1.0——P4 决策",
    "信封 id 生成与 queue/ids.js 格式重复实现（纯度门禁跨目录 import 所致，P5 统一）——沿用 P3a 债",
    "符号命名带领域味（sessionLockKey/orderedSessionEvents/genEventId）P5 中性化候选（破 API）——沿用 P1 债",
    "ordering.js 无专属测试——沿用 P1 债",
    "locks.js 形参 skillId 领域词——沿用 P1 债",
    "真实持久化 logStore/snapshotStore 适配器（照端口契约）尚未交付，随首个消费方落地——P3b 决策"
  ]
}
