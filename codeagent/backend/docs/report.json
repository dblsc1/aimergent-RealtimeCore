{
  "schema_version": 2,
  "module": "realtime_core",
  "role": "backend",
  "task": "realtime_core P3b：会话内核（下）——decide/evolve 聚合 + 事件版本化 upcaster + 崩溃重放运行时（defineAggregate/reject / upcastEvent 逐级升级缺函数响亮 throw / createMemorySnapshotStore / createAggregateRuntime execute 锁串行+CAS+滚动快照·load 快照+尾部重放 / append 路径唯一复用 delivery / 四不变量 property 测 / 三层全链路参考示例）",
  "tier": "hard",
  "status": "approved",
  "summary": "[backend 自检通过的 candidate，待 reviewagent 正式审核；分支未合 main] 新增 src/session/ 四生产文件：aggregate.js（defineAggregate 纯聚合描述 + reject/isReject 结构化业务拒绝，decide→事件[]｜reject、evolve 纯折叠、applyEvent=upcast+evolve；未知命令/非法返回=throw，未知事件默认响亮 throw 可配 ignore）；upcaster.js（upcastEvent 事件版本化：库拥有版本号强制盖 v=fromV+1、逐级 v→v+1、缺升级函数/来自未来响亮 throw，消费方永远只见最新 schema）；memory-snapshot-store.js（快照端口内存实现，防御性深拷贝 structuredClone）；aggregate-runtime.js（createAggregateRuntime：execute=locks.withLock('stream:id')→load→decide→append CAS→读回折叠→滚动快照，load=快照+尾部重放）。append 路径唯一：execute 复用 P3a delivery.publish（严格 expectedLastSeq CAS + wakeup.emit('appended')），全库仅一条写日志路径，CAS 冲突响亮 throw 不静默重试。四条不变量各有固定种子 property 测（重放确定性含快照 present/absent/behind 三形态、拒绝无痕、evolve 只见升级后事件、execute 串行等价且去锁反证 ConflictError）。reference/classroom-aggregate.ref.mjs 整库首次三层（聚合+投递+传输）串跑最小课堂全链路，含 v1→v2 事件演进。既有 110 测试零修改全绿，新增 43 全绿（153/153 串行），纯度门脚本未改、session scope 自动覆盖新四文件后 56/56 PASS。分支 feat/p3b-decide-evolve 不合 main。",
  "git": {
    "protocol": "embedded-self-v2",
    "branch": "feat/p3b-decide-evolve",
    "base": "9d8c03e15c854b0d0427b4e5333a0cefae3922ea",
    "head": "SELF",
    "diff_mode": "contains",
    "changed_files": [
      "code/backend/src/session/upcaster.js",
      "code/backend/src/session/aggregate.js",
      "code/backend/src/session/memory-snapshot-store.js",
      "code/backend/src/session/aggregate-runtime.js",
      "code/backend/src/session/aggregate.test.mjs",
      "code/backend/src/session/aggregate-runtime.test.mjs",
      "code/backend/src/session/aggregate-versioning.test.mjs",
      "code/backend/src/session/aggregate.property.test.mjs",
      "code/backend/reference/classroom-aggregate.ref.mjs",
      "code/backend/reference/classroom-aggregate.ref.test.mjs",
      "module_docs/contract.md",
      "module_docs/rules.md",
      "module_docs/handoff.md",
      "codeagent/backend/docs/worklog/2026-07-19-backend-p3b-decide-evolve.md",
      "codeagent/backend/docs/report.json"
    ]
  },
  "contract": { "touched": false, "which": [], "consumes": [] },
  "cross_module_impact": [],
  "escalation": null,
  "tests": {
    "unit": { "passed": 153, "failed": 0, "cmd": "cd code/backend && node --test --test-concurrency=1 (串行)" },
    "e2e": { "passed": 0, "failed": 0, "flow": "无 e2e：库模块，无服务进程/对外端点。三层全链路自证由 reference/classroom-aggregate.ref.test.mjs 承担（注入真实 engine.js longPoll）" }
  },
  "compat_gate": {
    "existing_tests_unmodified": true,
    "existing_pass": 110,
    "new_pass": 43,
    "evidence": "git diff --stat 9d8c03e -- code/ 对全部既有文件为空（本期全部为新增文件，无一既有源/测试被改）；110 既有零修改 + 43 新增 = 153 全绿"
  },
  "self_check": {
    "reviewcode_passed": true,
    "attempts": 1,
    "failed_gates": [],
    "passed": true,
    "detail": "check-kernel-purity.mjs 56 PASS / 0 FAIL（脚本未改；session scope 从 P3a 起递归覆盖 code/backend/src/session/*.js，新四生产文件自动纳入：transport 16 + session 40=8 文件×5 项——import 不出目录零 transport / 零扩展领域词 / 零 Date.now·Math.random / 零 db.transaction / ≤500 行；最大文件 aggregate.js 170 行）"
  },
  "invariants_mapping": [
    "不变量1（重放确定性：崩溃后 load 重建===一路 evolve；含快照 present/absent/behind 三形态）→ property①（250 种子×24 步，每步三条重建路径 rtSnap.load[快照,snapshotEvery=3 覆盖 present+behind]/新 rtNoSnap.load[absent 全量]/新 rtCrash.load[崩溃=新实例+幸存快照]均深比影子模型）",
    "不变量2（拒绝无痕：reject 不产事件/不改状态/不动日志）→ property①内嵌每步（被拒步日志长度不变+rejected.code 匹配影子）+ property②（200 种子，随机前缀后必被拒命令后日志长度/重建态纹丝不动）",
    "不变量3（decide/evolve 只见升级后事件：重放路径每事件 v===当前版本）→ property③（200 种子，直接写随机长度 v1 日志→v2 聚合 evolve 记录所见 v→断言全===2 且加权状态等影子）",
    "不变量4（execute 串行：并发 execute 同 stream 等价于串行、CAS 零冲突；出 ConflictError 即锁失效）→ property④（120 种子真锁：同发 k 个 add 不逐个 await，全成功+seq 连续+累计和守恒）+ property⑤（去锁反证：并发写触发响亮 ConflictError，证明锁正是串行化那道且冲突不被静默吞）"
  ],
  "versioning_evolution_tests": [
    "v1 日志写满→代码升级 v2→重放：旧事件经 upcaster 补 weight=1，状态正确（aggregate-versioning ①）",
    "同流混合 v1/v2 事件重放正确（②）",
    "v1→v2→v3 级联升级，weight+sign 逐级补齐（③）",
    "evolve 只见当前版本（spy 记录 v1/v2/v3 输入全被折叠为当前版本 3）（④）",
    "缺 upcaster 遇旧版本 = 响亮 throw 禁静默（⑤）",
    "事件来自未来 v>当前 = 响亮 throw 回滚场景（⑥）",
    "upcaster 返回非对象 = TypeError（⑦）",
    "库强制递增版本号：upcaster 忘 bump v 也不死循环（⑧）",
    "快照 schema 不匹配→丢弃、从日志全量重建，脏快照被隔离（⑨）"
  ],
  "full_chain_demo": "reference/classroom-aggregate.ref.mjs + .ref.test.mjs（4 用例全绿）：整库首次三层串跑。链路=teacher/student/parent 三组挂长轮询等待→老师 send(push-question)→runtime.execute 走 decide 守卫→question-pushed 事件→复用 delivery.publish append→wakeup.emit('appended')→三组 longPoll(真实 engine.js) 同时唤醒 respond.settled→三组独立游标推进。另证 decide 守卫拒绝无痕、断线重连仅凭 logStore+snapshot 重建聚合态逐字一致、v1→v2 事件演进(answer-submitted via 补 legacy)。",
  "deviations": [
    "append 路径唯一=复用 delivery.publish：runtime 内部 createDelivery({logStore, wakeup:wakeup??noopWakeup})，execute 调 delivery.publish(streamId,events,{expectedLastSeq}) 严格 CAS 分支冲突原样上抛——全库仅一条 append 路径（任务单 §2 硬约束）；wakeup 可选，纯聚合场景 no-op",
    "库拥有事件版本号：升级函数只变换 payload/形状，库强制盖 v=fromV+1，版本单调有硬保证、永不因忘 bump 死循环（比信任 upcaster 自设 v 更防呆）",
    "execute 内一处 await Promise.resolve(replay())：真实 logStore 适配器可能异步，此让点也令无锁并发真交错，使不变量 4 的 CAS 兜底可被 property⑤ 反证",
    "evolve 单一折叠路径：execute 追加后读回落盘信封用与 load 完全相同的 foldEvents(upcast→evolve) 推进——保证 execute 后内存态逐字等于崩溃重建态（不变量 1 由构造成立）",
    "未知命令=throw（编程错误）非 reject；未知事件默认 throw 可配 onUnknownEvent:'ignore'（前向兼容场景）",
    "快照 schema 版本不匹配→丢弃从日志全量重建（evolve 演进后旧快照 state 可能陈旧）；schemaVersion 缺省 1",
    "快照端口内存实现防御性深拷贝 structuredClone(on put+get)：护住确定性不变量，代价拷贝开销（真实持久化适配器天然序列化无此顾虑）；要求 state 可 structuredClone",
    "decide 可产出空事件数组=合法 no-op（返回当前状态不写日志，区别于 reject）",
    "snapshotEvery 滚动只看 seq 跨边界（Math.floor(new/every)>Math.floor(prior/every)），确定性不依赖时钟，缺省 50",
    "lightweight 模式下单 agent 按任务单显式授权一并更新 module_docs（contract/rules/handoff）；纯度门脚本本期未改（session scope 已覆盖）；下游仍有独立 reviewagent 审 candidate",
    "本仓无 git remote（dev 孵化局部仓，与 P1/P2/P3a 同状态）——candidate 停在本地 commit，无 push 环节"
  ],
  "tech_debt": [
    "sealEnvelopes 的 id 生成与 queue/ids.js 格式重复实现（纯度门禁跨目录 import 所致，P5 统一）——沿用 P3a 债",
    "符号命名带领域味（sessionLockKey/orderedSessionEvents/genEventId）P5 中性化候选（破 API）——沿用 P1 债",
    "ordering.js 无专属测试——沿用 P1 债",
    "locks.js 形参 skillId 领域词——沿用 P1 债",
    "真实持久化 logStore/snapshotStore 适配器（照端口契约）尚未交付，随首个消费方落地——P3b 决策"
  ]
}
