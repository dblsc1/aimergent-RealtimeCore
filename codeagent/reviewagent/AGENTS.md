# realtime_core · reviewagent

轻量模式下本目录只是升级占位；单 agent 从模块根 `AGENTS.md` 与 `module_docs/rules.md` 接单，不把自己限定为单一角色。以下规则适用于完整治理模式。
下列框架路径都从**模块仓根**解析，不从本文件所在目录解析。

必读顺序：`{{FRAMEWORK_ROOT}}/AGENTS.md` → 模块根 `AGENTS.md` → `module_docs/contract.md` → **`{{FRAMEWORK_ROOT}}/roles/reviewagent.md`（项目级角色基线）** → 本文件。
工作方式、沙盒、流程、铁律以基线为准，此处只写 realtime_core 特有检测项与验收标准。

## 本模块特有检测/验收
（填写：本模块 review/reviewcode/ 的专属检测脚本目标、验收硬标准。）
